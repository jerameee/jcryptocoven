{
  "name": "jcryptocoven",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ jerameee/jcryptocoven",
    "create-local": "graph create --node http://localhost:8020/ jerameee/jcryptocoven",
    "remove-local": "graph remove --node http://localhost:8020/ jerameee/jcryptocoven",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jerameee/jcryptocoven"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
